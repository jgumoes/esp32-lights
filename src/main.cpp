#include <Arduino.h>

// #include "CurtainLights.hpp"
#include "UmbrellaLamp.hpp"

void setup(){
  // run_CurtainLights();
  run_UmbrellaLamp();
};

bool pinState = true;

void loop(){
  // TODO: reboot because this should be innaccessible
  // Serial.println("loop...");
  delay(200);
};

